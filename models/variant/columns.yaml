# ===================================
#  List Column Definitions
# ===================================

columns:
    package:
        label: Package
        searchable: true

    description:
        label: Description
        searchable: true

    platform:
        label: Platform
        searchable: true
        relation: platform
        select: name

    is_maintenance:
        label: wwrf.mobileapp::lang.variant.is_maintenance
        comment: wwrf.mobileapp::lang.variant.is_maintenance_comment
        type: switch
